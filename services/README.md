# Backend Services

This directory contains the microservices for DevPilot 2026:

- auth-service → authentication, sessions, cookies
- projects-service → projects, feature requests, drafts
- gateway → Apollo Gateway (federation)

Each service should be independently runnable.
